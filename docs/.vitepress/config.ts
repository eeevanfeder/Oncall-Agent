import { defineConfig } from "vitepress";

export default defineConfig({
  title: "Super AI",
  description: "单仓骨架与验证说明",
  lang: "zh-CN",
  themeConfig: {
    nav: [{ text: "首页", link: "/" }],
  },
});
